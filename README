# Luwisp Notes

使用 Astro 和 TypeScript 构建的静态个人博客。开发阶段采用组件和内容集合，`npm run build` 最终输出可直接部署到 GitHub Pages 的 `dist/` 静态目录。

## 本地开发

要求 Node.js 22 或更高版本。

```bash
npm install
npm run dev
```

常用命令：

```bash
npm run check      # Astro 和 TypeScript 检查
npm run build      # 同步可用的 GitHub 数据并构建静态站
npm run preview    # 预览 dist 构建结果
```

## 内容管理

- 文章：`content/articles/**/*.md`，目录结构会映射到分类文件管理器与文章 URL。
- 目录名称与说明：`content/folders.json`。文章可省略 `category`，默认使用顶层目录名称。
- 站点与个人资料：`content/site.json`。
- 随机标语：`content/quotes.json`。
- 主题清单：`themes/index.json` 与 `themes/*.json`。
- 页面数量、归档分页及实验功能：`src/config/site.ts`。

新增文章时放入对应目录，并至少填写 `title`、`date` 与 `summary`。`tags`、`minutes` 和 `category` 都是可选项；目录用于主要组织，标签只承担跨目录关联。分类应用支持目录导航、搜索、排序、网格/列表视图和文件详情。

## 博客桌面架构

页面分为三层：壁纸与系统壳、由信息小组件和应用图标共同组成的主页桌面、归档/分类/阅读器应用窗口。

- 固定应用和顺序：`src/config/apps.ts`。主页、归档、分类始终固定；文章阅读器是唯一的动态应用槽位。
- 主页对象尺寸和位置：`src/config/widgets.ts`。新增小组件或小应用时先在这里声明桌面、平板、手机布局。
- 站点数据：`content/site.json`。文章和分类统计在静态构建阶段计算，不依赖浏览器接口。
- 桌面壳与应用窗口：`src/layouts/BaseLayout.astro`。
- 应用栏：`src/components/AppDock.astro`。桌面端固定在左侧，手机端将当前可见应用作为一个整体在底栏居中。

桌面主页由左右两个四列组和中央弹性间隔组成：组内对象共享固定网格线，4K 的额外空间只进入中央留白，因此跨列组件和单格应用可以严格对齐。平板使用 `4 × 5` 网格，手机使用两页 `4 × 5` 网格。网格允许刻意空出单元格，让壁纸参与构图。时间、最近文章等动态信息使用小组件，头像、主题、外观、GitHub 和邮件则使用独立应用图标。

手机主页使用横向分页；归档、分类和文章仍保持普通纵向滚动。打开过的文章会保留为一个阅读器图标，切换文章时只更新这个槽位，不会不断增加图标。阅读器标题栏左侧提供关闭按钮；关闭后会清除动态槽位并替换当前历史记录，因此刷新或后退不会立即把已关闭的文章重新打开。

## 页面切换动画

动画总开关位于 `src/config/site.ts`：

```ts
pageTransition: {
  enabled: true,
  durationMs: 320
}
```

将 `enabled` 改为 `false` 即可完全关闭客户端路由和页面动画。桌面端按应用栏顺序做纵向工作区切换：打开下方应用时新界面从下方进入，返回上方应用时从上方进入；同一阅读器内仅做轻量替换。转场使用 Astro `ClientRouter` 与浏览器 View Transition，样式集中在 `src/styles/global.css` 的 `app-screen` 转场区域。手机端和系统启用“减少动态效果”时始终不播放。

## GitHub 热力图

`scripts/fetch-github-contributions.mjs` 在构建阶段读取 GitHub GraphQL 的贡献日历，并写入 `src/data/contributions.json`。令牌不会进入浏览器产物。

GitHub Actions 会为每次工作流自动创建内置的 `GITHUB_TOKEN`，部署配置已将它传给同步脚本，因此线上构建不需要手动创建或保存令牌。账号名以 `content/site.json` 中的 `profile.githubUser` 为准；修改后重新运行部署工作流即可刷新热力图。

仅在本地手动同步时需要个人访问令牌：在 GitHub 头像菜单中进入 Settings → Developer settings → Personal access tokens → Fine-grained tokens → Generate new token。将 Resource owner 设为自己的账号，设置较短的有效期；读取公开贡献不需要额外写权限。令牌只应通过临时环境变量传入，不要写入站点配置、源码或提交到 Git。

本地临时同步：

```bash
GH_CONTRIBUTIONS_TOKEN=your_token npm run sync-data
```

部署工作流会在推送、手动执行以及每天北京时间约 10:20 运行。没有配置本地令牌时，本地构建会继续使用仓库中的现有快照。

## GitHub Pages

工作流位于 `.github/workflows/deploy-pages.yml`。在仓库 Settings → Pages 中将 Source 设为 GitHub Actions，然后把分支合并到 `main` 即可触发部署。

当前站点按自定义域名 `luwisp.blog` 配置，域名文件位于 `public/CNAME`，Astro 的 `site` 地址位于 `astro.config.mjs`。若改用 `*.github.io/<repo>/` 项目地址，需要同时删除 `public/CNAME` 并设置 Astro 的 `site` 和 `base`。

## 设计规范

完整视觉与交互基线见 `docs/reproducible-design.md`。

---

# 原始需求记录

## overview

一个个人博客，分为github page上的展示端和桌面上的管理端，目的是通过管理端

## 展示端

### 功能

1. 归档
    单独一个界面，以条式按时间排序文章

2. 分类
    根据类别分类文章

3. 主页
    展示多个小组件。小组件具体有：
3a. 头像、名字、标语
3b. github、email链接图标（后续可能会加）
3b. github提交绿点图
3c. 时间小组件
3d. 一个随机变化博客主题的组件
3e. 按照时间排序最近发的文章【在所有组件最下方】




4. 风格调整
    多个主题，如catpuccin各种颜色搭配、everforest等，具体要求：
4a. 位于侧边栏底部或者手机界面的顶部
4b. 顶栏和侧边栏展开才能看到
4c. 默认设置俩种主题分别为暗色主题和亮色主题，点击切换明暗主题
4d. 可以展开更多主题（此时侧边栏其他按钮消失），并选择其中主题作为亮色主题和暗色主题
4e. 主题要求在本地以文件形式保存，方便管理端修改，同时主题分为暗色和亮色两大类

### 风格要求

参考example/index.html，风格类似即可，不要完全照搬，你可以用其他架构或语言

在index.html上不同的点，侧边栏按扭居中。

### 其他参照

github page一个具体博客可以参考

example/jekyll-theme-chirpy

## 管理端

暂时不做

但展示端要方便管理端添加修改文章，修改壁纸和风格
